"use client";

import { Button } from "~/components/ui/button";

export function LoginUserButton() {
  return (
    <Button className="my-2 w-full max-w-[400px] font-bold" size="lg">
      Login as Admin
    </Button>
  );
}
